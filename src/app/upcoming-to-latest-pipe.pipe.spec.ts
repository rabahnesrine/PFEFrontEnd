import { UpcomingToLatestPipePipe } from './upcoming-to-latest-pipe.pipe';

describe('UpcomingToLatestPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UpcomingToLatestPipePipe();
    expect(pipe).toBeTruthy();
  });
});
